# NextGenMedia Configuration
# Copy this file to .env and fill in your values

# ============ Application Settings ============
DEBUG=false
API_HOST=127.0.0.1
API_PORT=8888

# ============ Security Settings ============
# IMPORTANT: Set a unique, secure key for cookie encryption in production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
COOKIE_ENCRYPTION_KEY=your-secure-random-key-here

# ============ Whisper Settings ============
# Model options: tiny, base, small, medium, large
# Larger models = better accuracy but slower
WHISPER_MODEL=base
# Device: cpu or cuda (requires NVIDIA GPU)
WHISPER_DEVICE=cpu

# ============ Translation Settings ============
# google, deepl, gpt, claude, or deepseek
TRANSLATION_SERVICE=google

# API Keys for paid translation services (only needed if using that service)
# OPENAI_API_KEY=sk-xxx          # For GPT translation
# ANTHROPIC_API_KEY=sk-ant-xxx   # For Claude translation
# DEEPSEEK_API_KEY=sk-xxx        # For DeepSeek translation
# DEEPL_API_KEY=xxx              # For DeepL translation

# ============ TTS Settings ============
# edge (Microsoft Edge TTS - free and high quality)
TTS_SERVICE=edge
# Chinese voices: zh-CN-XiaoxiaoNeural, zh-CN-YunxiNeural, zh-CN-YunyangNeural
TTS_VOICE=zh-CN-XiaoxiaoNeural

# ============ Bilibili Credentials ============
# Get these from browser cookies after logging in
# Open DevTools (F12) > Application > Cookies
BILIBILI_SESSDATA=
BILIBILI_BILI_JCT=
BILIBILI_BUVID3=

# ============ Douyin Credentials ============
# Full cookie string from browser
# Open DevTools (F12) > Network > Any request > Copy "Cookie" header
DOUYIN_COOKIES=

# ============ Xiaohongshu Credentials ============
# Full cookie string from browser
XHS_COOKIES=

# ============ Redis (Optional, for task queue) ============
REDIS_URL=redis://localhost:6379/0
